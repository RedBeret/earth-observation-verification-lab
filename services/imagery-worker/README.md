# Imagery worker

Consumes versioned imagery-ingestion events. The executable handler is added with the
verified ingestion workflow; the Stage 2 container establishes lifecycle and dependency
behavior.
