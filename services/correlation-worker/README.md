# Correlation worker

Consumes accepted telemetry events. The executable correlation handler is added with the
verified event workflow; the Stage 2 container establishes lifecycle and dependency
behavior.
